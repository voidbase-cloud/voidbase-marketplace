**Template submission for `voidbase-cloud/voidbase-site`**

Audited at `376c0d83f00d`:

- PASS  **public repository** — reachable without signing in
- PASS  **not archived** — actively owned
- FAIL  **has a licence** — GitHub cannot identify one, so nobody can tell what they may do with it. A LICENSE file GitHub does not recognise counts as none here.
- PASS  **has a commit to point at** — 376c0d83f00d on master
- PASS  **looks like a voidbase project** — found void.json, vb_hooks, vb_migrations, vb_secrets
- PASS  **has a README** — present
- PASS  **nothing obviously alarming in the files we read** — read package.json, README.md, void.json, vite.config.mts

Nothing blocking. A maintainer decides from here; the checks above are a first pass and not a guarantee.
