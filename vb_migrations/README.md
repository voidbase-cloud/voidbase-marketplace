# vb_migrations

Collections for this project's own instance, as code, applied once and in order.

Empty for the same reason `vb_hooks` is: the registry lives in git, not in a collection. The first migration here
will be whichever part of the marketplace stops being a file.

See https://voidbase.cloud/docs/run/stack/migrations.
